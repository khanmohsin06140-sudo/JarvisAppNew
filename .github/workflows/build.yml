ACT AS A SENIOR ANDROID BUILD ENGINEER, KOTLIN EXPERT, JETPACK COMPOSE EXPERT, AND GITHUB ACTIONS EXPERT.

I have uploaded these files to this GitHub repository:

- Code.zip
- generate_jarvis.py

Repository:
JarvisAppNew

IMPORTANT:
I need a REAL BUILDABLE Android APK.

DO NOT only explain the errors.
DO NOT tell me that I should fix them manually.
DO NOT stop after editing files.

YOU MUST:
1. Extract the project.
2. Inspect every Android/Gradle/Kotlin file.
3. Fix all compilation errors.
4. Configure GitHub Actions correctly.
5. Run the Gradle build.
6. Continue fixing errors until the Debug APK builds successfully.
7. Upload the APK as a GitHub Actions artifact.

==================================================
PART 1 — EXTRACT Code.zip CORRECTLY
==================================================

Code.zip is currently uploaded to the repository.

Extract Code.zip.

If the ZIP contains:

JarvisMohsinCore/

then move the Android project contents to the repository root.

The final repository MUST NOT become:

JarvisAppNew/JarvisMohsinCore/JarvisMoh
