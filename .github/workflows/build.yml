You are an expert Senior Android Build Engineer and Kotlin/Jetpack Compose Architect.

I have uploaded TWO files to this GitHub repository:

1. Code.zip
2. generate_jarvis.py

Repository name: JarvisAppNew

YOUR JOB IS TO BUILD THE APK, NOT JUST EXPLAIN HOW TO BUILD IT.

IMPORTANT:
Do not leave Code.zip as the only project source.
Do not expect me to manually unzip anything.
You must perform all required repository modifications yourself.

STEP 1 — EXTRACT THE PROJECT
--------------------------------------------------
Extract Code.zip into the repository.

After extraction, inspect the ZIP structure carefully.

If Code.zip contains a top-level JarvisMohsinCore/ directory, move its Android project contents to the repository root so that the repository ultimately looks approximately like:

JarvisAppNew/
├── .github/
│   └── workflows/
│       └── build.yml
├── app/
│   ├── build.gradle.kts
│   └── src/
├── gradle/
│   └── wrapper/
│       └── gradle-wrapper.properties
├── build
