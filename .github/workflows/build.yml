I have uploaded a ZIP file created by ChatGPT for my website project.

ZIP FILE:
GurudevCollection_Code.zip

IMPORTANT:
Do NOT treat the ZIP file as a single file to serve.
You MUST extract/unzip the ZIP first.

I want the extracted project to be placed inside this repository's `app/` folder.

Required structure:

repository-root/
└── app/
    ├── package.json
    ├── index.html
    ├── vite.config.js
    ├── tailwind.config.js
    ├── postcss.config.js
    ├── src/
    ├── supabase/
    ├── public/ (if included)
    └── other project files

IMPORTANT:
The ZIP itself contains the actual Vite project at its root. After extraction, `app/package.json` MUST exist directly inside the app folder.

Do NOT create:
app/GurudevCollection_Code/
app/gurudev-collection/
app/project/
or any other extra nested project folder.

After extracting:

1. Inspect all extracted files.
2. Preserve the complete source code.
3. Do not truncate, remove, rename, or replace working files unnecessarily.
4. Run:
   cd app
   npm install
   npm run build
5. Fix any build errors automatically.
6. Make sure the final website successfully builds with Vite.
7. The website must be deployable on Vercel.
8. Configure SPA routing correctly so `/admin` works after deployment.
9. Keep the existing React + Vite + Tailwind architecture.
10. Do NOT convert the project to another framework.

Business information:

Brand:
Gurudev Collection

Location:
Chhatrapati Sambhajinagar (Aurangabad), Maharashtra

Store area:
Shahganj

Landmark:
HP Petrol Pump, Shahganj

Use this location information in the Store Locator, footer, contact section and local SEO content.

Do not invent an exact street number or fake phone number.

The final result must be a working website inside `app/`.
