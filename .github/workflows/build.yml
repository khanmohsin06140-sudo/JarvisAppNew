Act as a Senior Full-Stack React Architect, DevOps Engineer and GitHub Actions expert.

I have a ZIP file created by ChatGPT for my website:
GurudevCollection_Code.zip

IMPORTANT:
1. Find the ZIP file in the repository.
2. Extract/unzip it automatically.
3. The extracted website project MUST be placed directly inside:

app/

FINAL REQUIRED STRUCTURE:

repository/
├── app/
│   ├── package.json
│   ├── index.html
│   ├── vite.config.js
│   ├── tailwind.config.js
│   ├── postcss.config.js
│   ├── src/
│   ├── supabase/
│   └── ...
└── .github/
    └── workflows/
        └── build.yml

CRITICAL:
`app/package.json` MUST exist directly inside `app`.

NEVER create:

app/GurudevCollection_Code/package.json
app/gurudev-collection/package.json
app/project/package.json

If the ZIP contains a parent folder, remove that unnecessary nesting and put the actual project files directly into `app/`.

Do not delete or truncate valid source code.

After extraction, inspect the complete project.

PROJECT:
Gurudev Collection

LOCATION:
Chhatrapati Sambhajinagar (Aurangabad), Maharashtra
Shahganj
HP Petrol Pump area

Do not invent an exact street number or phone number.

TECHNOLOGY:
- React
- Vite
- Tailwind CSS
- React Router
- Lucide React
- Supabase-ready
- Vercel-ready

WEBSITE REQUIREMENTS:
- Premium luxury clothing design
- Matte black #0a0a0a
- Charcoal #141414
- Metallic gold #D4AF37
- White/off-white
- Mobile-first responsive design
- Product catalog
- Categories
- Product cards
- Product detail modal
- Product image/video support
- Sizes
- Price/MRP/discount
- Stock status
- WhatsApp ordering
- Store location
- Admin page
- Supabase integration
- SEO for Chhatrapati Sambhajinagar/Aurangabad/Shahganj

If a video URL exists, preserve:

<video
  autoPlay
  loop
  muted
  playsInline
  preload="metadata"
/>

WhatsApp ordering must use encodeURIComponent().

Never expose Supabase service-role/private keys.

After placing the project inside `app`, verify the files.

Then run:

cd app
npm install
npm run build

If there are build errors, FIX THEM AUTOMATICALLY.

Do not just report the error.

Run the build again after every fix until successful.

The final build MUST generate:

app/dist/index.html

IMPORTANT:
Do NOT put this instruction text inside `.github/workflows/build.yml`.

Now configure/fix GitHub Actions for the Gurudev Collection website.

IMPORTANT:
The workflow file is:

.github/workflows/build.yml

It MUST contain ONLY valid GitHub Actions YAML.

Do NOT paste natural-language instructions into build.yml.

Replace the existing invalid workflow if necessary.

Use this exact workflow:

name: Build Gurudev Collection Website

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: app

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Install dependencies
        run: npm install

      - name: Build website
        run: npm run build

      - name: Verify build output
        run: test -f dist/index.html

      - name: Upload website build
        uses: actions/upload-artifact@v4
        with:
          name: gurudev-collection-dist
          path: app/dist

Before running the workflow, verify:

app/package.json
app/index.html
app/src/
app/vite.config.js

MUST EXIST.

There must NOT be an extra nested project directory.

Correct:
app/package.json

Wrong:
app/GurudevCollection_Code/package.json

Then execute the build.

If npm install or npm run build fails:
1. Read the actual error.
2. Fix the source/configuration.
3. Run the build again.
4. Continue until the build succeeds.

Do not hide errors.
Do not skip verification.
Do not create fake files just to make the workflow green.

Final requirement:

The GitHub Actions build must successfully produce:

app/dist/index.html

The generated `dist` folder must be uploaded as the GitHub Actions artifact named:

gurudev-collection-dist

The website must remain compatible with Vercel deployment.
